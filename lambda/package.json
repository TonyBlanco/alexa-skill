{
  "name": "alexa-skill-lambda",
  "version": "0.2.0",
  "description": "Compañero diario: skill Alexa para personas mayores (ASK SDK)",
  "main": "index.js",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "ask-sdk-core": "^2.14.0",
    "ask-sdk-model": "^1.86.0"
  },
  "devDependencies": {
    "ask-sdk-local-debug": "^1.1.0"
  }
}
